name: "Hello Actions"
description: "Greet someone"

inputs:
  BRANCH_NAME:
    description: "Branch being closed"
    required: true
    default: "DEFAULT"

  MY_NAME:
      description: "Who to greet"
      required: true
      default: "WORLD"    

runs:
  using: "docker"
  image: "Dockerfile"